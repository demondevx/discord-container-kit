# Installation

Grab the package from npm:

```bash
npm install discord-container-kit
```

### Requirements
* Node.js v20.0.0 or higher
* discord.js v14.18.0 or higher

This library relies heavily on the latest discord.js v14 release for Discord API typings. Make sure your base discord.js dependency is up to date, or you'll run into missing enum errors.
